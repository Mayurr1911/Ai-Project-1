# Finance Stock Prediction

This project uses Linear Regression to predict stock prices based on historical data.

## Requirements
- Python 3.x
- Install dependencies: `pip install -r requirements.txt`

## Usage
Run the model.py script to train and save the model.

## Dataset
Uses synthetic stock price data for demonstration.
